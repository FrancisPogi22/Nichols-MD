{{ 'philosophy.css' | asset_url | stylesheet_tag }}

<section id="philosophy">
  <div class="wrapper">
    <div class="philosophy-container">
      <div class="philosophy-image-container">
        {% if section.settings.featured_image != blank %}
          <img
            src="{{ section.settings.featured_image | img_url: "2048x" }}"
            alt="{{ section.settings.featured_image.alt }}"
            loading="lazy"
          >
        {% else %}
          <img
            src="https://cdn.shopify.com/s/files/1/0727/8949/1929/files/Default_Image.webp?v=1733414521"
            alt="Default Image"
            loading="lazy"
          >
        {% endif %}
      </div>
      <div class="philosophy-details-container">
        <img
          src="https://cdn.shopify.com/s/files/1/0727/8949/1929/files/Flower.webp?v=1733414521"
          class="flower"
          alt="Flower"
          loading="lazy"
        >
        {% if section.settings.lead_in_text != blank %}
          <p class="low-white lead-in">{{ section.settings.lead_in_text | escape }}</p>
        {% endif %}
        {% if section.settings.heading_text != blank %}
          <h2 class="white">{{ section.settings.heading_text | escape }}</h2>
        {% endif %}
        {% if section.settings.sub_text != blank %}
          <p class="low-white">
            {{ section.settings.sub_text | escape }}
          </p>
        {% endif %}
        {% for block in section.blocks %}
          {% case block.type %}
            {% when 'buttons' %}
              {% if block.settings.primary_button_link_text != blank %}
                <a
                  {% if block.settings.primary_button_link == blank %}
                    role="link" aria-disabled="true"
                  {% else %}
                    href="{{ block.settings.primary_button_link }}"
                  {% endif %}
                  class="btn-primary"
                >
                  {{ block.settings.primary_button_link_text | escape }}
                </a>
              {% endif %}
          {% endcase %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Philosophy",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Lead In Text"
    },
    {
      "type": "text",
      "id": "lead_in_text",
      "default": "About Our Philosophy",
      "label": "Lead In Text"
    },
    {
      "type": "header",
      "content": "Heading Text"
    },
    {
      "type": "text",
      "id": "heading_text",
      "default": "It’s More Than Skin Deep",
      "label": "Heading Text"
    },
    {
      "type": "header",
      "content": "Sub Text"
    },
    {
      "type": "text",
      "id": "sub_text",
      "default": "This space is reserved for your unique and captivating header text. Let your creativity flow and set the tone for what follows. Your header is the gateway to your content – make it compelling and unforgettable. Add your header text now!",
      "label": "Sub Text"
    },
    {
      "type": "header",
      "content": "Featured Image"
    },
    {
      "type": "image_picker",
      "id": "featured_image",
      "label": "Featured Image"
    }
  ],
  "blocks": [
    {
      "type": "buttons",
      "name": "Buttons",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Primary Button"
        },
        {
          "type": "text",
          "id": "primary_button_link_text",
          "label": "Primary Button Link Text",
          "default": "Learn More",
          "info": "Leave the label blank to hide the button."
        },
        {
          "type": "url",
          "id": "primary_button_link",
          "label": "Primary Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Philosophy"
    }
  ]
}
{% endschema %}
